// Web client connection config.
// Set `serverUrl` to force an exact endpoint, for example:
//   "wss://playpalace.example.com:7000"
// If `serverUrl` is empty, app.js derives host/scheme from window location.
// Optional `serverPort` overrides the derived/default port in that mode.
export const WEB_CLIENT_CONFIG = {
  serverUrl: "",
  serverPort: 7000,
};

